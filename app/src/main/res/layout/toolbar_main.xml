<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:focusableInTouchMode="true"
    android:gravity="center_vertical"
    android:orientation="vertical">

    <EditText
        android:id="@+id/et_main_toolbar_search"
        style="@style/search_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:background="@drawable/selector_et_search_title"
        android:cursorVisible="false"
        android:drawableLeft="@drawable/icon_search"
        android:focusableInTouchMode="false"
        android:hint="请输入搜索内容"
        android:padding="6dp"
        android:visibility="gone" />

    <TextView
        android:id="@+id/tv_main_toolbar_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:gravity="center"
        android:textColor="@color/white"
        android:textSize="@dimen/title_text_size"
        android:visibility="visible"
        tools:text="Title" />

    <Button
        android:id="@+id/btn_main_toolbar_right"
        style="@android:style/Widget.Material.Toolbar.Button.Navigation"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_alignParentRight="true"
        android:gravity="center"
        android:textColor="@color/white"
        android:textSize="@dimen/search_text_size"
        android:visibility="gone"
        tools:targetApi="lollipop"
        tools:text="Text" />

</RelativeLayout>